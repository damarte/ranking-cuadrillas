name: Schedule probe (diagnostico temporal)

on:
  schedule:
    - cron: '3-59/15 * * * *'
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PROBE OK $(date -u)"
